{% extends 'page.html.twig' %}

{% block content %}
{{ parent() }}

<p>To allow someone you authorize to borrow and/or place holds on materials in your name, we need the following:</p>
<ul>
<li>Please fill in the form below.</li>
<li><em>Permission time granted should not exceed one year.</em></li>
<li>Inform the authorized person that the staff at the Circulation Desk will ask them to present their photo ID at the time they are checking out materials in your name.</li>
<li>If at any time you have questions regarding these procedures or you would like to have an updated list of materials checked out in your name, please call 419-372-2051 or email <a href="mailto:circdesk@bgsu.edu">circdesk@bgsu.edu</a>.</li>
</ul>
<p>The form will be sent to the library circulation desk and copied to the two people named below. The agreement is active as soon as that email is received.</p>
<p><strong>It will be your responsibility</strong> to communicate to your authorized borrower any guidelines and/or limitations within which they may operate.</p>
<p>Thank you for your cooperation and continued support of BGSU University Libraries.</p>

<form method="post" class="bgsu_common_form">
<fieldset>
<legend>Authorization granted by:</legend>
<div class="bgsu_common__flex bgsu_common__gap">
<div class="bgsu_common_form_group">
<label for="granted_by_name">Name</label>
<div><input type="text" name="granted_by_name" id="granted_by_name" value="{{ post['granted_by_name'] }}" required></div>
</div>
<div class="bgsu_common_form_group">
<label for="granted_by_id">ID Number</label>
<div><input type="text" name="granted_by_id" id="granted_by_id" value="{{ post['granted_by_id'] }}" required></div>
</div>
<div class="bgsu_common_form_group">
<label for="granted_by_email">Email <small>(Enter @bgsu.edu addresses only)</small></label>
<div><input type="email" name="granted_by_email" id="granted_by_email" value="{{ post['granted_by_email'] }}" required pattern=".*(@bgsu\.edu)"></div>
</div>
</div>
<input type="checkbox" name="granted_by_permission" id="granted_by_permission" value="1" {% if post['granted_by_permission'] %}checked {% endif %}required>
<label for="granted_by_permission">I understand that checking this box constitutes an electronic signature confirming that I acknowledge and agree to the above terms.</
label>
</fieldset>

<fieldset>
<legend>Authorization granted to:</legend>
<div class="bgsu_common__flex bgsu_common__gap">
<div class="bgsu_common_form_group">
<label for="granted_to_name">Name</label>
<div><input type="text" name="granted_to_name" id="granted_to_name" value="{{ post['granted_to_name'] }}" required></div>
</div>
<div class="bgsu_common_form_group">
<label for="granted_to_id">ID Number</label>
<div><input type="text" name="granted_to_id" id="granted_to_id" value="{{ post['granted_to_id'] }}" required></div>
</div>
<div class="bgsu_common_form_group">
<label for="granted_to_email">Email</label>
<div><input type="email" name="granted_to_email" id="granted_to_email" value="{{ post['granted_to_email'] }}" required></div>
</div>
</div>
</fieldset>

<p>
<label for="expire">This authorization is to <strong>expire</strong></label>
<input type="date" name="expire" id="expire" value="{{ post['expire'] }}" required min="{{ 'now'|date('Y-m-d') }}" max="{{ 'now'|date_modify('+1 year')|date('Y-m-d') }}">.
(Time granted should not exceed one year).
</p>
<p><strong>I understand that by granting permission, I am assuming responsibility for all materials checked out in my name.</strong></p>
<p><button type="submit" class="bgsu_common_button bgsu_common_button__primary">Email Your Request</button></p>
</form>

{% endblock %}
